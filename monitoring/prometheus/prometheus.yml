global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["prometheus:9090"]

  # Example: scrape bot if it exposes metrics at /metrics on 8080
  - job_name: bot
    metrics_path: /metrics
    static_configs:
      - targets: ["bot:8080"]


